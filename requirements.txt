# Dependencias del pipeline ETC 88 (scripts/run_all.sh).
# Instalar:  pip install -r requirements.txt   (además de Node para verify.py: node --check)
openpyxl        # lee/escribe los .xlsx (presupuesto, equipo, flujo, tripulación)
python-docx     # genera los .docx (asesores, carpeta, cartas, reunión finanzas)
cairosvg        # build_doc_trabajo_88.py: rasteriza SVG -> PNG (necesita libcairo2 en el SO)
pillow          # manejo de imágenes (logo/firma) si algún builder lo requiere
